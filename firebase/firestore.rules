rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client access to the users collection.
    // Only Cloud Functions using the Admin SDK can read/write user documents.
    match /users/{uid} {
      allow read, write: if false;
    }
  }
}
